Need to have a gui (based on the egui crate) which has two main panes horizintally side by side:
1. In the left pane, An editor based on the  egui-code-editor crate where a user can enter statements in the
TROY language. The editor should have syntax highlighting (and a later possbility for  auto-completion
     features.)

2. A right pane where a graphical display  (to be done later) of a graph generated from the TROY langauage can be displayed